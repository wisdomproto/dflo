# Python cache
__pycache__/
*.pyc

# Source lab report images (patient data; do not commit)
랩데이터/
판독문/
판독문_ocr/
판독문_review/
판독문_캡쳐.zip
판독문_캡쳐_/
igg4 검사/
xray 이미지 (jpg)/
솔찍썰 치료후기 케이스/
0. backup/

# OCR cached output + parsed lab JSONs (PHI; regeneratable from images)
랩데이터_ocr/

# Excel seeds with patient identifiers
성장치료 환자 리스트.xls
치료 후기 케이스 정리*.xlsx
치료 후기 케이스 정리 (정리완료).xlsx
patients.json
seed_patients.sql

# Ad-hoc OCR sample dumps
*.surya.json
*.raw.json

# Patient roster JSON (PHI: names, RRN, address)
_patient_roster.json

# Ad-hoc inspection scripts (may print PHI to stdout)
_inspect_*.mjs
_verify_*.mjs
_region_tmp.mjs

# Patient story work files (PHI: names, measurements, prescriptions)
_patient_contexts.json
_stories_chunk*.json

# 치료사례 후보 검토 산출물 (원장검토용 md/html 은 실명 포함 PHI, 상세프로필은 생성물 — 전부 로컬 전용)
# 재생성: node cases/select_case_candidates.mjs · node cases/gen_case_profiles.mjs
케이스후보_*.md
케이스후보_*.html

# 원장 스토리 작성용 입력 덤프 (가명이지만 생성물 — 로컬 전용)
_story_inputs.json
