# 187 성장케어 AI 상담 - 원클릭 설치 가이드

## 🚀 **이제 딱 하나만 실행하면 됩니다!**

### **Windows 사용자**

1. **파일 탐색기**에서 `scripts` 폴더 열기
2. **`setup_and_run.bat`** 더블클릭
3. 끝! 🎉

```bash
# 또는 명령 프롬프트에서:
cd scripts
setup_and_run.bat
```

---

### **Mac/Linux 사용자**

```bash
cd scripts
chmod +x setup_and_run.sh
./setup_and_run.sh
```

---

## 📋 **자동으로 실행되는 작업들**

✅ **[1/4] API 키 설정** (1초)
- `.env` 파일 자동 생성
- API 키: `AIzaSyBI8J3EJSlEG7pLH3pAVrFZFq6_4Mbu-q8`

✅ **[2/4] 패키지 설치** (10-30초)
```
google-generativeai
PyPDF2
Flask
flask-cors
python-dotenv
numpy
```

✅ **[3/4] PDF 처리** (1-3분)
- `우리 아이 키 성장 바이블 원고.pdf` 자동 탐지
- 임베딩 생성 및 저장
- `knowledge_base.json` 생성

✅ **[4/4] 서버 실행** (즉시)
- Flask API 서버 시작
- `http://127.0.0.1:5000` 에서 대기

---

## 🌐 **웹앱 열기**

서버가 시작되면 **새 터미널**을 열고:

```bash
# 프로젝트 루트 폴더에서
python -m http.server 8000

# 브라우저에서 열기
open http://localhost:8000/info.html
```

---

## 🎯 **전체 과정 (처음부터 끝까지)**

### **1단계: 파일 준비**
```
프로젝트/
  ├── scripts/
  │   ├── setup_and_run.bat  ← 이 파일 실행!
  │   ├── pdf_to_rag_free.py
  │   └── local_api_server.py
  └── 우리 아이 키 성장 바이블 원고.pdf
```

### **2단계: 스크립트 실행**
```bash
# Windows
scripts\setup_and_run.bat

# Mac/Linux
scripts/./setup_and_run.sh
```

**출력 예시:**
```
====================================
187 성장케어 AI 자동 설치
====================================

[1/4] API 키 설정 중...
✓ API 키 설정 완료

[2/4] Python 패키지 설치 중...
✓ 패키지 설치 완료

[3/4] PDF 파일 처리 중...
PDF 파일: ../우리 아이 키 성장 바이블 원고.pdf
총 250페이지 처리 중...
✓ PDF 처리 완료

[4/4] API 서버 시작 중...
✓ 서버가 http://127.0.0.1:5000 에서 실행됩니다

====================================
설치 완료! 이제 웹앱을 열어주세요:
http://localhost:8000/info.html
====================================

 * Running on http://127.0.0.1:5000
```

### **3단계: 웹 서버 실행** (새 터미널)
```bash
python -m http.server 8000
```

### **4단계: 브라우저에서 열기**
```
http://localhost:8000/info.html
```

---

## 🆘 **문제 해결**

### **Q: Python이 없다고 나와요**
```bash
# Python 설치 확인
python --version

# Python 3.7 이상 필요
# 없다면: https://www.python.org/downloads/
```

### **Q: PDF 파일을 못 찾아요**
PDF 파일 위치 확인:
```
프로젝트_루트/
  └── 우리 아이 키 성장 바이블 원고.pdf  ← 여기!
```

또는 직접 경로 지정:
```bash
cd scripts
python pdf_to_rag_free.py
# 입력: C:\Users\...\우리 아이 키 성장 바이블 원고.pdf
```

### **Q: 포트가 이미 사용 중이에요**
```bash
# 포트 변경
python local_api_server.py --port 5001
```

---

## 🎉 **완료 확인**

### **1. API 서버 테스트**
새 터미널에서:
```bash
curl -X POST http://127.0.0.1:5000/api/chat \
  -H "Content-Type: application/json" \
  -d "{\"message\": \"성조숙증이란?\"}"
```

### **2. 웹앱에서 테스트**
1. `info.html` 열기
2. **🤖 AI 성장 상담** 버튼 클릭
3. 질문 입력 후 엔터

---

## 📂 **생성되는 파일들**

실행 후 자동 생성:
```
scripts/
  ├── .env                    ← API 키
  ├── knowledge_base.json     ← RAG 데이터 (임베딩 포함)
  └── (기타 캐시 파일들)
```

---

## 💡 **팁**

### **서버 백그라운드 실행** (선택)
```bash
# Windows
start /B python local_api_server.py

# Mac/Linux
nohup python3 local_api_server.py &
```

### **자동 재시작 설정**
서버가 죽으면 자동 재시작:
```bash
# while 루프 사용
while true; do python3 local_api_server.py; sleep 1; done
```

---

## 🔒 **보안 주의사항**

⚠️ **중요**: `.env` 파일에 API 키가 저장됩니다!
- Git에 커밋하지 마세요
- `.gitignore`에 추가되어 있는지 확인

```bash
# .gitignore에 추가
echo ".env" >> .gitignore
echo "knowledge_base.json" >> .gitignore
```

---

## 📞 **추가 도움**

문제가 발생하면:
1. 에러 메시지 복사
2. 콘솔 로그 확인
3. 질문 남겨주세요!

**이제 정말로 딱 하나만 실행하면 됩니다!** 🎯

```bash
# Windows
scripts\setup_and_run.bat

# Mac/Linux  
scripts/./setup_and_run.sh
```
