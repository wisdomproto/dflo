out/
node_modules/
sources/
# 작업 영상 (대용량·재생성 가능: yt-dlp 다운로드 + LatentSync 립싱크 출력)
public/videos/*-src.mp4
public/videos/*-lipsync-*.mp4
public/videos/*-panel.mp4
.env
.env.local

# main clip source video (large) — only the asset dirs, NOT src/mainclip code
/public/mainclip/
/mainclip/

# 대용량 작업 소스 (원본 다운로드·푸티지·TTS — 재생성/재다운로드 가능, src/shorts 코드와 무관)
/assets/
/shorts/
/public/footage/
/public/audio/narration/
# 쇼츠 TTS 클론음성 wav (gen-tts-short 재생성 가능) + 다운로드/생성 이미지(인포그래픽 등, R2 원본)
/public/audio/shorts/
/public/images/*/
scripts/_tmp-*.txt

# calc 광고 입력 이미지(s1)·나레이션 TTS/믹스 (재생성 가능, CalcDemoTH 선례) — 영상은 ffmpeg mux 후처리라 컴포넌트 미참조
/public/ads/
/public/audio/ads/

# 치료사례 스토리 릴스 일러스트(AI-gen, 매트3D 재생성 예정)·TTS/믹스 — 재생성 가능, CaseStoryReel.tsx 코드는 tracked
/public/casestory/
/public/audio/casestory/
